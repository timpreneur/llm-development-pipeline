# dev-pipeline-build — lessons

Dated, append-only. One `lessons.md` for the whole Build skill. Tag entries by mode in the heading: `code-opt`, `security`, `accessibility`, `UX polish`, `QA`, `legal`, `marketing`.

Add only when a **repeatable pattern** surfaces — not one-offs.

Entry format:

```
## YYYY-MM-DD — <project> — <mode> — <short title>
Pattern: <what repeats>
Evidence: <which features>
Promotion candidate: yes | no | watch
Notes: <optional>
```

LLM Ops promotes to project CLAUDE.md, not this skill. Never edit or remove entries below — append only.

---
