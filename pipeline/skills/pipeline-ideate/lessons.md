# pipeline-ideate — lessons

Dated, append-only. Add only when a **repeatable pattern** surfaces — not one-offs.

Entry format:

```
## YYYY-MM-DD — <project> — <short title>
Pattern: <what repeats>
Evidence: <which features>
Promotion candidate: yes | no | watch
Notes: <optional>
```

**Promotion candidate** — flag for LLM Ops review:
- `yes` — pattern is stable, belongs in project CLAUDE.md (or global) next pass.
- `no` — locally interesting, not promotable.
- `watch` — maybe; needs more evidence before promoting.

LLM Ops promotes, not this skill. Never edit or remove entries below — append only.

---
